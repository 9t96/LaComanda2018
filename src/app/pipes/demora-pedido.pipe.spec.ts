import { DemoraPedidoPipe } from './demora-pedido.pipe';

describe('DemoraPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new DemoraPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
