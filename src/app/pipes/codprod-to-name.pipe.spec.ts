import { CodprodToNamePipe } from './codprod-to-name.pipe';

describe('CodprodToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CodprodToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
