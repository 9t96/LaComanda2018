import { PrecioProductoPipe } from './precio-producto.pipe';

describe('PrecioProductoPipe', () => {
  it('create an instance', () => {
    const pipe = new PrecioProductoPipe();
    expect(pipe).toBeTruthy();
  });
});
